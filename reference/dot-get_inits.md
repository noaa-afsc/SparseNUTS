# Get a single initial value vector in untransformed model space

Get a single initial value vector in untransformed model space

## Usage

``` r
.get_inits(init, obj2, seed, inputs)
```

## Arguments

- init:

  The initial value strategy

- obj2:

  The joint TMB model

- seed:

  RNG seed

- inputs:

  A list as returned by `.get_inputs`.
